Drop these two files here before deploying:

  hero-video.mp4   <- your product-in-motion footage (loops, muted, no sound needed)
  hero-poster.jpg  <- a still frame shown before the video loads / if it fails

Hero.tsx already points at these exact filenames — no code changes needed
once they're in place.
